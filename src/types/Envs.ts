export interface Envs {
  Port: string;
  intra_secret: string;
  intra_endpoint: string;
  redirect_uri: string;
  client_id: string;
  encryption_key: string;
  backend_endpoint: string;
  intra_point: string;
}